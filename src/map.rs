// map-related code
use crate::prelude::*;

const NUM_TILES: usize = (SCREEN_WIDTH * SCREEN_HEIGHT) as usize;

// super:: accesses the module immediately above your module in the tree.
// crate:: accesses the root of the tree, main.rs .
